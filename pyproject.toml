[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "market_spoofing_detector"
version = "0.1.0"
description = "High Frequency Market Spoofing Detector"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]